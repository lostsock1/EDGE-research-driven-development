# operator-specific values — never commit
template.env
# cross-agent shared-state file some operators keep at the clone root
/RESUME.md
# operator audit/working notes kept at the clone root
/notes/
# render output — regenerated by install.sh
rendered/

# Git clones — each has its own .git and is tracked in its own repo
/RAG/
/NAIRRATOR/
/projects/rag/RAG/
/projects/rag/RAG-p1c-real-ingestion/

# EDGE/openclaw runtime + sync state (machine/sync-specific, never port)
/.openclaw/
/storage/
/snapshots/
/.qdrant-initialized
/openclaw-workspace-state.json
*.log

# Separate sub-vault (kai) — track in its own repo if desired
/kai-vault/

# Obsidian machine-local state (keep config; ignore open-tab/cache)
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache/

# Secrets
.env
*.env

# Lab experiment output (runtime state, never port)
lab/experiments/

# OS / editor
.DS_Store
*.swp
*~

# python bytecode
__pycache__/
