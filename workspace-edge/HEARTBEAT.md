# Heartbeat — {{AGENT_NAME}}

_No active heartbeat tasks._

<!--
  Heartbeat tasks have been migrated to cron-based scheduling.
  This file is now a status/notes file only — heartbeat tasks
  (e.g. PR gate sweeps) are defined as cron jobs via the Gateway
  cron API and executed by shared-scripts/edge-pr-gate.sh.
  See docs/OPERATIONS.md §The PR gate for the gate protocol.
-->
